version: '1'

services:
  parser-demo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: parser-demo
    ports:
      - "8050:8050" 
    restart: always
    environment:
      - CONTAINERNAME=parser-demo
    env_file:
      - ./.env